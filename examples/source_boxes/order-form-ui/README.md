# Order Form UI

A deliberately small, read-only Source Box containing the form surface for an order estimate.
It has no runtime script; Reweave can extract its typed inputs, action, output, and styles as one UI behavior capsule.
