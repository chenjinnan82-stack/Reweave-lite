# Reweave Examples

These folders are tiny public Source Boxes for first-run testing.

Use one in the desktop app:

1. Start Reweave with `./start_reweave_static.sh`.
2. Click `Bind Source Box`.
3. Choose one folder under `examples/source_boxes/`.

They are read-only inputs. Reweave should not write back into them.
