# v0.1.7 - Manual capsules and small project packs

- 5 public Source Boxes for reproducible demos.
- Manual capsule selection with `--list-capsules` and `--select-capsule`.
- Small Project Pack output with `index.html`, `styles.css`, `app.js`, `task_pack.json`, `capsules_used.json`, `provenance.json`, and `snippets_used.json`.
