# v0.1.9 - Source Box to Small Project Pack

- Desktop workflow smoke: Source Box onboarding, hidden unsafe actions, and Task Pack preview bridge path.
- Public task profiles for dashboard, landing page, form tool, admin panel, data viewer, portfolio viewer, operations panel, and artist landing.
- 10 local non-demo Source Box acceptance cases passed as usable with provenance and `snippets_used.json`.
- Public demo selection prefers capsules that can produce bounded source snippets.
- Scanner ignores `.venv*` dependency folders when choosing sample paths.

Boundary: source folders stay read-only; no automatic multi-file source writes are enabled.
